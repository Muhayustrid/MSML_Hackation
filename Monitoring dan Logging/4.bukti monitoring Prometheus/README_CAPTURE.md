# Prometheus Evidence

Capture five Bank Marketing metric queries from Prometheus: `model_up`, `prediction_requests_total`, `prediction_latency_seconds`, `process_memory_bytes`, and `exporter_uptime_seconds`.
